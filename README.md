# AgenticAI-Foundations-to-Advanced

## 🧪 Development

```bash
# Install dev dependencies
uv sync --all-groups

# Run tests
uv run pytest -s tests

# Run linting
uvx pre-commit run --all-files

# Type checking
uv run mypy src --strict
```
